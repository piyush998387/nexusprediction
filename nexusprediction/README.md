# NexusPrediction

Aviator Crash Prediction App — deployed via Railway.

## Deploy

1. Push this repo to GitHub
2. Connect to Railway → New Project → Deploy from GitHub
3. Railway auto-detects Node.js and runs `npm start`
